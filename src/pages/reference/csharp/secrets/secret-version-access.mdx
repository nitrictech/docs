export const description =
  "Reference for Nitric's .NET library - Retrieves the value from a version of a secret."

# .NET - Secret.Version.Access()

Retrieves the value from a version of a secret.

```csharp
using Nitric.Sdk.Resource;

var keyRef = Nitric.Sdk.Nitric.Secret("apiKey").With(SecretPermission.Accessing);

var keyValue = keyRef.Latest().Access();

keyValue.ValueText;

Nitric.Sdk.Nitric.Run();
```

## Examples

### Access the latest version of a secret

```csharp
using Nitric.Sdk.Resource;

var keyRef = Nitric.Sdk.Nitric.Secret("apiKey").With(SecretPermission.Accessing);

var keyValue = keyRef.Latest().Access();

keyValue.ValueText;

Nitric.Sdk.Nitric.Run();
```

### Access a specific version of a secret

```csharp
using Nitric.Sdk.Resource;

var keyRef = Nitric.Sdk.Nitric.Secret("apiKey").With(SecretPermission.Accessing);

var keyValue = keyRef.Version("the-version-id").Access();

keyValue.ValueText;

Nitric.Sdk.Nitric.Run();
```
