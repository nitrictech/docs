export const description =
  "Reference for Nitric's JVM library - Gets a reference to a child collection on a document."

# JVM - collection.doc.collection()

Gets a reference to a child collection on a document.

<CodeGroup>

```java
import io.nitric.Nitric;
import io.nitric.resources.CollectionPermission;

class User {
  String name;
  int age;

  public User(String name, int age) {
    this.name = name;
    this.age = age;
  }
}

public class Application {
  public static void main(String[] args) {
    var profiles = Nitric.INSTANCE.collection("profiles", User.class).with(CollectionPermission.Read);

    var drakesProfile = profiles.doc("Drake Mallard");

    var drakesEnemies = drakesProfile.collection("enemies");

    Nitric.INSTANCE.run();
  }
}
```

```kotlin
import io.nitric.Nitric
import io.nitric.resources.CollectionPermission

data class User(val name: String, val age: Int)

fun main() {
  val profiles = Nitric.collection("profiles", User::class.java).with(CollectionPermission.Read)

  val drakesProfile = profiles.doc("Drake Mallard")

  val drakesEnemies = drakesProfile.collection("enemies")

  Nitric.run()
}
```

</CodeGroup>

## Parameters

<Properties>
  <Property name="name" required type="string">
    The name of the child collection to reference
  </Property>
</Properties>

### Notes

Document collection relationships can be at most 1 deep.

```javascript
import { collection } from '@nitric/sdk'

const profiles = collection('profiles').for('reading')

profiles
  .doc('Drake Mallard')
  .collection('enemies')
  .doc('Steel Beak') // ✔️ We can go this deep
  .collection('enemies') // ❌ But not this deep
```

<CodeGroup>

```java
import io.nitric.Nitric;
import io.nitric.resources.CollectionPermission;

class User {
  String name;
  int age;

  public User(String name, int age) {
    this.name = name;
    this.age = age;
  }
}

public class Application {
  public static void main(String[] args) {
    var profiles = Nitric.INSTANCE.collection("profiles", User.class).with(CollectionPermission.Read);

    profiles
      .doc("Drake Mallard")
      .collection("enemies")
      .doc('Steel Beak') // ✔️ We can go this deep
      .collection('enemies') // ❌ But not this deep

    Nitric.INSTANCE.run();
  }
}
```

```kotlin
import io.nitric.Nitric
import io.nitric.resources.CollectionPermission

data class User(val name: String, val age: Int)

fun main() {
  val profiles = Nitric.collection("profiles", User::class.java).with(CollectionPermission.Read)

  profiles
    .doc("Drake Mallard")
    .collection("enemies")
    .doc('Steel Beak') // ✔️ We can go this deep
    .collection('enemies') // ❌ But not this deep

  Nitric.run()
}
```

</CodeGroup>

### See also

- [collection()](./collection)
