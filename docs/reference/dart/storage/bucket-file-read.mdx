export const description =
  "Reference for Nitric's Dart library - Read the contents of a file from a bucket."

# Dart - bucket.file.read()

Read the contents of a file from a bucket.

```dart
import 'package:nitric_sdk/nitric.dart';

final assets = Nitric.bucket("assets").allow([
  BucketPermission.read,
]);

final logo = assets.file("images/logo.png");

final logoData = await logo.read();
```

## Examples

### Read a file

```dart
import 'package:nitric_sdk/nitric.dart';

final assets = Nitric.bucket("assets").allow([
  BucketPermission.read,
]);

final logo = assets.file("images/logo.png");

final logoData = await logo.read();
```
