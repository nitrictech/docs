
Nitric provides a command-line interface (CLI) to assist with various tasks when building applications with Nitric. To view a list of available commands, you can call the CLI without specifying any subcommands or arguments:

```bash
nitric
```

Amoung other things, this will provide a list of `TOPICS` and `COMMANDS`. Commands are named CLI operations to perform tasks, such as `build` & `run`, these commands each have their own set of arguments and flags/options to control their behavior. Topics are collections of related commands, for example the `make` topic will contain subcommands for each resource that can be made, e.g. `make:function`.

### Help and Documentation

Each command is self documented and provides a "help" interface describing the usage, arguments and options for the command. Use the `help` command to view the help information for any other command:

```bash
# Example displaying help for the `build` command
nitric help build
```

### Plugins

In addition to the default commands provided by the base CLI, additional commands can be added via plugins. By default, provider specific commands e.g. `deploy:azure` are added via provider plugins, this ensures the size of the CLI remains smaller when only targeting a subset of providers, it also enables new providers or community supported providers to be added without changing the base CLI.

To view a list of currently installed plugins, run the `plugins` command directly:

```bash
nitric plugins
```

To install a new plugin use the `install` subcommand:

```bash
# Example of installing the default AWS provider plugin
nitric plugins:install @nitric/plugin-aws
```

To update plugins use the `update` subcommand:

```bash
nitric plugins:update
```

## Core Commands

  
### nitric build

build a project


#### Arguments

<table>
    <th>
       <strong>Argument</strong>
    </th>
    <th>
       <strong>Required</strong> 
    </th>
    <th>
       <strong>Description</strong>
    </th>
    <tr>
      <td>directory</td>
      <td>
          No
      </td>
      <td>The project directory to build from</td>
    </tr>
</table>



#### Options

<table>
    <th>
       <strong>Option</strong>
    </th>
    <th>
       <strong>Required</strong> 
    </th>
    <th>
       <strong>Description</strong>
    </th>
    <tr>
      <td>--ci</td>
      <td>
          No
      </td>
      <td>
        build a project
      </td>
    </tr>
    <tr>
      <td>--help</td>
      <td>
          No
      </td>
      <td>
        show CLI help
      </td>
    </tr>
    <tr>
      <td>--file</td>
      <td>
          No
      </td>
      <td>
        the stack definition file (default: .&#x2F;nitric.yaml)
      </td>
    </tr>
    <tr>
      <td>--provider</td>
      <td>
          No
      </td>
      <td>
        the targeted provider for this build
      </td>
    </tr>
</table>


#### Examples

```bash
$ nitric build .
```
  
  
### nitric doctor

check environment for config and prerequisite software



#### Options

<table>
    <th>
       <strong>Option</strong>
    </th>
    <th>
       <strong>Required</strong> 
    </th>
    <th>
       <strong>Description</strong>
    </th>
    <tr>
      <td>--ci</td>
      <td>
          No
      </td>
      <td>
        check environment for config and prerequisite software
      </td>
    </tr>
    <tr>
      <td>--help</td>
      <td>
          No
      </td>
      <td>
        show CLI help
      </td>
    </tr>
</table>


#### Examples

```bash
$ nitric doctor
```
  
  
### nitric run

builds and runs a project locally for testing


#### Arguments

<table>
    <th>
       <strong>Argument</strong>
    </th>
    <th>
       <strong>Required</strong> 
    </th>
    <th>
       <strong>Description</strong>
    </th>
    <tr>
      <td>directory</td>
      <td>
          No
      </td>
      <td>The project directory to build from</td>
    </tr>
</table>



#### Options

<table>
    <th>
       <strong>Option</strong>
    </th>
    <th>
       <strong>Required</strong> 
    </th>
    <th>
       <strong>Description</strong>
    </th>
    <tr>
      <td>--ci</td>
      <td>
          No
      </td>
      <td>
        builds and runs a project locally for testing
      </td>
    </tr>
    <tr>
      <td>--help</td>
      <td>
          No
      </td>
      <td>
        show CLI help
      </td>
    </tr>
    <tr>
      <td>--file</td>
      <td>
          No
      </td>
      <td>
        the stack definition file (default: .&#x2F;nitric.yaml)
      </td>
    </tr>
    <tr>
      <td>--provider</td>
      <td>
          No
      </td>
      <td>
        the targeted provider for this build
      </td>
    </tr>
</table>


#### Examples

```bash
$ nitric run
```
  
  
### nitric make:stack

Creates a new Nitric stack


#### Arguments

<table>
    <th>
       <strong>Argument</strong>
    </th>
    <th>
       <strong>Required</strong> 
    </th>
    <th>
       <strong>Description</strong>
    </th>
    <tr>
      <td>name</td>
      <td>
          No
      </td>
      <td>the name of the new stack to create</td>
    </tr>
    <tr>
      <td>template</td>
      <td>
          No
      </td>
      <td>Service template</td>
    </tr>
</table>



#### Options

<table>
    <th>
       <strong>Option</strong>
    </th>
    <th>
       <strong>Required</strong> 
    </th>
    <th>
       <strong>Description</strong>
    </th>
    <tr>
      <td>--ci</td>
      <td>
          No
      </td>
      <td>
        Creates a new Nitric stack
      </td>
    </tr>
    <tr>
      <td>--help</td>
      <td>
          No
      </td>
      <td>
        show CLI help
      </td>
    </tr>
    <tr>
      <td>--force</td>
      <td>
          No
      </td>
      <td>
        force the stack to be created, even in a folder with existing contents.
      </td>
    </tr>
</table>


#### Examples

```bash
$ nitric make:stack
```
  

> For more documentation on our CLI, you can <a target="_target" href="https://github.com/nitrictech/cli">view the GitHub</a> Repository

## Plugin Commands

You can view the plugin commands, for deployment for example over in the [Plugin Commands Section](/docs/reference/cli/plugin-commands)